# 2026-02-13 작업 로그

## 전투 시스템 (Battle System)
- **무기 아이콘 표시 수정**:
  - `BattlePage.tsx`: 장착된 무기가 없을 때, 기본 '검' 이미지가 아닌 빈 값("")을 전달하도록 변경.
  - `PlayerPanel.tsx`: 무기 아이콘이 없을 경우, 이미지 태그 자체를 렌더링하지 않도록 하여 빈 슬롯처럼 보이게 수정.

## 상점 시스템 (Shop System)
- **생명의 수정 (Life Crystal) 구매 로직 개선**:
  - `shop.service.ts`: 'HEART' 아이템 구매 시 인벤토리에 추가하지 않고 즉시 효과가 적용되도록 변경.
  - 최대 체력(Max HP) +20 증가.
  - 현재 체력(Current HP) +20 회복 (즉시 적용).
  - `maxHpBonusCount`를 증가시켜 다음 구매 시 가격이 상승하도록 처리.
